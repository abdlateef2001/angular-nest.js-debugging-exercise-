{
  "name": "blog-app-backend",
  "version": "1.0.0",
  "description": "Backend for the blog application using NestJS",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "watch": "tsc -w -p tsconfig.build.json",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "@nestjs/platform-express": "^9.0.0",
    "rxjs": "^7.0.0",
    "reflect-metadata": "^0.1.13",
    "typescript": "^4.5.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^9.0.0",
    "ts-jest": "^27.0.0",
    "jest": "^27.0.0",
    "ts-loader": "^9.0.0",
    "jest-preset-angular": "^9.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}